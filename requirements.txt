# Versions this code is tested against.  torch >= 1.3 is a hard floor: the diversity term
# uses torch.pca_lowrank, which does not exist before it.
torch==2.0.1
torchvision==0.15.2
numpy==1.26.0
scipy==1.11.3
pillow==9.4.0
pyyaml==6.0.1

# Only ImageNet needs pandas, to read the CSV manifests; every other dataset works without it.
# pandas==2.0.3
