# The objective as written in the paper, for reproduction.
# CIFAR10, 10 images/class.  This is the protocol the reported number was measured with:
# 20000 iterations, 3 independent condensation run(s), 10 networks evaluated per run.

inherit: _base.yaml
name: paper_cifar10_ipc10

data:
  dataset: CIFAR10
  ipc: 10

condense:
  iterations: 20000
  num_exp: 3

eval:
  models: [ConvNet]
  num_eval: 10

output:
  save_path: runs/paper_cifar10_ipc10
